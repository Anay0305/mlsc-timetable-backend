"""HTTP server for the MLSC timetable backend.

See BACKEND_PLAN.md (workspace root) for the phased rollout. This package wraps
the `timetable_parser` library in a FastAPI app that reads from `data/` on disk.
"""
